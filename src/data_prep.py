from config.constants import *
from config.utils import *

data = DATA_INPUTS['main_dataset']

def data_prep(df: pd.DataFrame, infer: bool = True, verbose: bool = True):
    logger.info('This code is still in development...')

if __name__ == '__main__':
    data_prep(df=data, infer=True, verbose=True)